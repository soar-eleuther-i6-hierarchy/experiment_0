# Server credentials / access notes — never commit
.ssh
.server-access.md

# SSH keys
*.pem
id_ed25519
id_ed25519.pub
id_rsa
id_rsa.pub

# Python
__pycache__/
*.py[cod]
venv/
.venv/
*.egg-info/
outputs/*.log

# Stage-01 tensor caches (~700 MB/layer). Hosted on the Hub instead, see README:
# huggingface.co/datasets/soar-eleuther-i6-hierarchy/experiment_0-stats
outputs/**/*.pt
outputs/*.pt

# Generated proof-figures (rebuilt by reporting/make_report_figures.py into each
# run dir's figures/; never tracked)
figures/

# OS
.DS_Store
.env
MEETING_NOTES/

# Team SAE-training repo, cloned locally for Tier-2 training (its own git repo)
sae-training/

# Local-only work: tests + run artifacts, kept out of git
tests_local/
outputs_local/
REPORT_layer6.md
scoring/
toygen/
METRICS_MATRIX.md
PROPERTIES.md
score_trained.py